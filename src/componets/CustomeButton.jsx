import React from 'react'

const CustomeButton = ( {handleNewQuote} ) => {
  return (
    <button className='Button' onClick= {handleNewQuote}>Change curiosity</button>
  )
}

export default CustomeButton